export const selectCurrentBarber = (state) => state.barber.currentBarber;

export const selectLastClient = (state) => state.barber.lastClient;

export const selectLastBooking = (state) => state.barber.lastBooking;
