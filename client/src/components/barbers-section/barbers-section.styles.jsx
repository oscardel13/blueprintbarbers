import { styled } from "styled-components";

export const BarberDirectorySection = styled.section`
    overflow-x: hidden;
    .titlecontainer{
        text-align: center;
        padding:4rem;
        h2{
            font-size: 4rem;
            font-weight: 700;
        }
    }
`